<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |d| %>
<ul>
  <li><%= d.ingredient.name %></li>
  <p><%= d.description %></p>
  <p><%= link_to 'Delete dose', cocktail_dose_path(d), method: :delete %></p>
</ul>
<% end %>
<p><%= link_to 'Add a dose', new_cocktail_dose_path(@cocktail)%></p>
